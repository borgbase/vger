[package]
name = "vykar-common"
version = "0.12.12"
edition = "2021"
license = "GPL-3.0-only"

[lints]
workspace = true

[target.'cfg(unix)'.dependencies]
nix = { version = "0.31", features = ["user"] }
